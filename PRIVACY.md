# 隐私策略 / Privacy Policy

> 银狐木马检测 - Virus Detector（以下简称"本扩展"）

**最后更新日期 / Last updated：2026-07-10**

---

## 概述

本扩展致力于保护您的隐私。我们**不收集、不存储、不出售**任何个人身份信息。以下说明本扩展涉及的全部数据流动。

## 我们收集什么

### 我们不收集的

- ❌ 不收集个人身份信息（姓名、邮箱、密码等）
- ❌ 不收集浏览历史记录
- ❌ 不收集 Cookie、表单数据、剪贴板内容
- ❌ 不嵌入任何第三方分析或广告 SDK
- ❌ 不进行用户行为追踪或画像
- ❌ 不将任何数据出售或共享给第三方商业机构

### 我们处理的数据

本扩展仅在以下场景涉及数据流动，全部出于安全检测这一唯一目的：

#### 1. 域名查询（RDAP / WHOIS / DNS）

**目的**：查询访问网站的域名注册信息（注册时间），用于评估域名可信度。

**传输内容**：仅当前访问网站的**域名**（非完整 URL，不含路径、参数、Cookie 等）。

**目标服务器**：
- IANA RDAP 引导服务（`data.iana.org`）
- 各顶级域注册局 RDAP 服务器（由 IANA 引导文件指定）
- `rdap.ss` 代理服务（作为 RDAP 不可用时的回退）
- Google DNS-over-HTTPS（`dns.google`）—— 仅用于查询公共后缀列表（PSL），不解析完整域名

**存储**：不存储。查询结果缓存于内存，Service Worker 终止后即清除。

#### 2. 用户主动上报

**目的**：您可选择将误报或钓鱼网站上报给开发者，帮助改进检测规则。

**传输内容**：域名、风险评分、检测详情、扩展版本号和上报时间戳（由您的点击操作触发）。

**目标服务器**：Cloudflare Worker（`virus-detector-report.lolitide.workers.dev`），仅用于在 [GitHub](https://github.com/Lolitide/VirusDetector) 创建公开 Issue。

**存储**：不在本扩展中存储。上报数据以 GitHub Issue 形式公开可见。

> ⚠️ 此功能完全由您手动触发（点击"此报告有误"或两次点击"鉴定为钓鱼"）。不会自动上报。

#### 3. 版本更新检查

**目的**：检查是否有新版本可用。

**传输内容**：仅 HTTP 请求头中的基本信息（浏览器 User-Agent），不包含用户数据。

**目标服务器**：GitHub API（`api.github.com`）。

#### 4. 本地存储

以下数据**仅**存储在您本地的浏览器存储（`chrome.storage.local`）中，**永不离开您的设备**：

- 检测结果缓存（24 小时后自动清除）
- 您手动添加的白名单域名
- 您拦截后自动记录的下载域名黑名单（90 天自动清理）
- 扩展设置项

---

## 数据共享与披露

本扩展**不与任何第三方共享数据**，以下技术必需场景除外：

| 第三方 | 数据类型 | 目的 |
|--------|---------|------|
| IANA / 各注册局 RDAP 服务器 | 域名 | 查询域名注册信息 |
| Google DNS | 公共后缀查询（PSL） | 正确提取网站主域名 |
| GitHub | 您手动上报的域名和检测信息 | 创建公开 Issue 供开发者处理 |

以上第三方均不受我们控制，请参阅各自的隐私政策。

---

## 权限说明

本扩展申请的浏览器权限及其唯一用途：

| 权限 | 用途 |
|------|------|
| `activeTab` | 读取当前标签页 URL 以进行安全检测 |
| `storage` | 本地存储检测缓存、白名单和设置 |
| `downloads` | 监听下载事件，拦截疑似恶意文件的下载 |
| `scripting` | 注入内容脚本以分析页面内容 |
| `notifications` | 对高风险网站发送桌面通知 |
| `webNavigation` | 监听页面导航事件以触发安全检测 |
| `alarms` | 预留定时任务权限（当前未使用） |

---

## 您的控制权

- **白名单**：任何网站均可通过弹窗一键加入白名单，完全跳过所有检测和网络请求
- **不上报**：上报功能完全由您手动触发，不点击则不会发送任何数据
- **卸载即清除**：卸载扩展后，所有本地存储数据将被浏览器自动删除

---

## 联系我们

如有任何隐私相关问题，请通过以下方式联系：

- 📧 邮箱：`x2027235787@outlook.com`
- 🐙 GitHub Issues：<https://github.com/Lolitide/VirusDetector/issues>

---

*本隐私策略可能随扩展功能更新而调整，重大变更将通过扩展更新说明告知。*
